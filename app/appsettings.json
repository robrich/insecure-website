{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "//": "If doing this for real, use User Secrets in development and environment variables in production, and leave this config blank",
  "// ": "Server=. - the database is on this machine, TrustedConnection=True - use Windows Authentication to SQL Server"
  "ConnectionStrings": {
    "DefaultConnection": "Server=.;Database=InsecureWebsite;Trusted_Connection=True"
  }
}
